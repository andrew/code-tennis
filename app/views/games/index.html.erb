<h1>Active Games</h1>

<ul>
  <% @games.each do |game| %>
    <li>
      <h3><%= link_to game, game.pages_url %></h3>
      <p>Repo: <%= link_to game.repo_url, game.repo_url %></p>
    </li>
  <% end %>
</ul>
